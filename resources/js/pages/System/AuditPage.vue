<template><AppShell :title="pageTitle"><template #default><v-list lines="three"><v-list-item v-for="l in logs" :key="l.id" :title="`${l.domain} · ${l.action}`" :subtitle="`${l.user?.name??'시스템'} · ${l.description??''} · ${l.created_at}`"/></v-list><v-empty-state v-if="!logs.length" title="감사 로그가 없습니다." icon="mdi-history"/></template></AppShell></template>
<script setup>import{onMounted,ref}from'vue';import AppShell from'../../components/layout/AppShell.vue';const pageTitle='감사 로그';const logs=ref([]);onMounted(async()=>{logs.value=(await window.axios.get('/tillwhite/api/audits')).data.logs;});</script>
